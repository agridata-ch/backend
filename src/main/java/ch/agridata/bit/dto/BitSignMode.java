package ch.agridata.bit.dto;

/**
 * Indicates whether the BIT signing process requires user interaction (INTERACTIVE) or runs fully server-side (DIRECT).
 *
 * @CommentLastReviewed 2026-04-09
 */
public enum BitSignMode {
  INTERACTIVE,
  DIRECT
}
